# Todo List

The following items are left to implement as part of the platform offering

- Pub/Sub via WSS & P2P
    - Link to ...
- Full MySQL8 compliance
- Triggers & Events

Additional points to close

- Check paging works using WAL during Affinity
- Return (max 1000) records during queries via console or WSS unless bounds explicitly set
- Server Default (atm "root") user on installation first-time
- Support different password mechanisms (other than md5??) 
