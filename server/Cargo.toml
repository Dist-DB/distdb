[package]
name = "server"
version = "0.1.0"
edition = "2024"
authors = ["Sam Colak <sam@samcolak.com>"]

[dependencies]
serverlib = { path = "../serverlib" }
connector = { path = "../connector" }
common = { path = "../common" }